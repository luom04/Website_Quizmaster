import { PlaceholderPage } from "@/components/common/placeholder-page";

export function AdminSuspiciousAttemptsPage() {
  return (
    <PlaceholderPage
      eyebrow="Admin"
      title="Cảnh báo gian lận"
      description="Trang xem tab blur, copy attempt, right click và các event đáng nghi."
    />
  );
}

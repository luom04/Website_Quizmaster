import { PlaceholderPage } from "@/components/common/placeholder-page";

export function LoginPage() {
  return (
    <PlaceholderPage
      eyebrow="Public"
      title="Đăng nhập"
      description="Trang login sẽ được làm thật ở feature/auth."
    />
  );
}
